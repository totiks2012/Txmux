#!/bin/bash
# tx.sh v3.5 — Мастер-оркестратор (Прокси-режим)

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Просто передаем управление движку. 
# Теперь движок сам решит, когда и где запустить Тень.
bash "$DIR/txmux_03.sh" "$@"